name: 🚀 Быстрый деплой Dev окружения

on:
  push:
    branches: [ dev ]
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Принудительный деплой без проверок'
        required: false
        default: false
        type: boolean

env:
  SERVER_HOST: 195.14.122.135
  DEV_PROJECT_PATH: /var/www/estimate-app-dev
  DEV_BACKEND_PORT: 8001
  DEV_FRONTEND_PORT: 3001

jobs:
  quick-deploy-dev:
    name: 📦 Быстрый деплой в Dev
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔍 Checkout кода
      uses: actions/checkout@v4
      with:
        fetch-depth: 2

    - name: 🔑 Настройка SSH ключа
      uses: webfactory/ssh-agent@v0.8.0
      with:
        ssh-private-key: ${{ secrets.DEV_SSH_PRIVATE_KEY }}

    - name: 🌐 Добавление сервера в known_hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H ${{ env.SERVER_HOST }} >> ~/.ssh/known_hosts

    - name: 📋 Анализ изменений
      id: changes
      run: |
        echo "Анализ изменений между коммитами..."
        
        # Получаем список измененных файлов
        CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD || echo "")
        echo "changed_files<<EOF" >> $GITHUB_OUTPUT
        echo "$CHANGED_FILES" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
        # Определяем типы изменений
        BACKEND_CHANGED=false
        FRONTEND_CHANGED=false
        MIGRATIONS_CHANGED=false
        
        if echo "$CHANGED_FILES" | grep -E '\.(py|txt)$'; then
          BACKEND_CHANGED=true
        fi
        
        if echo "$CHANGED_FILES" | grep -E '\.(js|jsx|ts|tsx|json|css|html)$'; then
          FRONTEND_CHANGED=true
        fi
        
        if echo "$CHANGED_FILES" | grep -E 'migrations/'; then
          MIGRATIONS_CHANGED=true
        fi
        
        echo "backend_changed=$BACKEND_CHANGED" >> $GITHUB_OUTPUT
        echo "frontend_changed=$FRONTEND_CHANGED" >> $GITHUB_OUTPUT
        echo "migrations_changed=$MIGRATIONS_CHANGED" >> $GITHUB_OUTPUT
        
        echo "🔍 Обнаруженные изменения:"
        echo "Backend: $BACKEND_CHANGED"
        echo "Frontend: $FRONTEND_CHANGED" 
        echo "Migrations: $MIGRATIONS_CHANGED"

    - name: 🚀 Деплой на dev сервер
      run: |
        ssh -o StrictHostKeyChecking=no ubuntu@${{ env.SERVER_HOST }} << 'ENDSSH'
          set -e
          
          # Цвета для логов
          RED='\033[0;31m'
          GREEN='\033[0;32m'
          YELLOW='\033[1;33m'
          BLUE='\033[0;34m'
          NC='\033[0m'
          
          log() {
            echo -e "${BLUE}[$(date '+%H:%M:%S')]${NC} $1"
          }
          
          success() {
            echo -e "${GREEN}[SUCCESS]${NC} $1"
          }
          
          error() {
            echo -e "${RED}[ERROR]${NC} $1"
            exit 1
          }
          
          log "🚀 Начало быстрого деплоя dev окружения"
          
          # Переходим в директорию проекта
          cd ${{ env.DEV_PROJECT_PATH }}
          
          # Проверяем текущий статус
          log "📊 Текущий статус проекта:"
          git status --short || true
          git log --oneline -3 || true
          
          # Обновляем код с принудительным pull
          log "📥 Принудительное обновление кода из ветки dev..."
          git fetch origin dev
          git reset --hard origin/dev
          success "Код обновлен до последнего коммита"
          
          # Пересборка Docker контейнеров
          log "🐳 Пересборка Docker контейнеров..."
          
          # Остановка старых контейнеров
          log "⏹️ Остановка старых контейнеров..."
          docker stop estimate-backend-dev estimate-frontend-dev 2>/dev/null || echo "Контейнеры уже остановлены"
          docker rm estimate-backend-dev estimate-frontend-dev 2>/dev/null || echo "Контейнеры уже удалены"
          
          # Сборка нового backend образа
          log "🔨 Сборка backend образа..."
          docker build -t estimate-app-backend:dev-new ./backend/ || error "Ошибка сборки backend образа"
          success "Backend образ собран"
          
          # Сборка нового frontend образа  
          log "🎨 Сборка frontend образа..."
          docker build -t estimate-app-frontend:dev-new ./frontend/ \
            --build-arg VITE_API_BASE_URL=https://dev.app.iqbs.pro/api/v1 || error "Ошибка сборки frontend образа"
          success "Frontend образ собран"
          
          # Создание директории логов если не существует
          mkdir -p /var/www/estimate-app-dev/logs
          
          # Запуск нового backend контейнера с теми же настройками
          log "🚀 Запуск нового backend контейнера..."
          docker run -d --name estimate-backend-dev \
            --restart unless-stopped \
            --network bridge \
            -p ${{ env.DEV_BACKEND_PORT }}:8000 \
            -v /var/www/estimate-app-dev/logs:/app/logs \
            -e SECRET_KEY=dev-secret-key-for-testing \
            -e DATABASE_URL=postgresql://estimate_user:secure_password_123@host.docker.internal:5432/estimate_app_db \
            -e ALLOWED_HOSTS=dev.app.iqbs.pro,localhost,127.0.0.1 \
            -e CORS_ALLOWED_ORIGINS=https://dev.app.iqbs.pro,http://dev.app.iqbs.pro \
            -e CSRF_TRUSTED_ORIGINS=https://dev.app.iqbs.pro,http://dev.app.iqbs.pro \
            -e SSL_ENABLED=True \
            -e VITE_API_BASE_URL=https://dev.app.iqbs.pro/api/v1 \
            -e DJANGO_SETTINGS_MODULE=core.settings_production \
            --add-host=host.docker.internal:host-gateway \
            estimate-app-backend:dev-new || error "Ошибка запуска backend контейнера"
          success "Backend контейнер запущен"
          
          # Запуск нового frontend контейнера
          log "🌐 Запуск нового frontend контейнера..."
          docker run -d --name estimate-frontend-dev \
            --restart unless-stopped \
            --network bridge \
            -p ${{ env.DEV_FRONTEND_PORT }}:80 \
            estimate-app-frontend:dev-new || error "Ошибка запуска frontend контейнера"
          success "Frontend контейнер запущен"
          
          # Обновление тегов образов для совместимости
          log "🏷️ Обновление тегов образов..."
          docker tag estimate-app-backend:dev-new estimate-app-backend:dev || true
          docker tag estimate-app-frontend:dev-new estimate-app-frontend:dev-fix || true
          
          # Перезагрузка Nginx для очистки кеша
          log "🔄 Перезагрузка Nginx..."
          sudo nginx -s reload || error "Ошибка перезагрузки Nginx"
          success "Nginx перезагружен"
          
          # Очистка старых образов
          log "🧹 Очистка старых образов..."
          docker image prune -f || true
          
          # Ожидание запуска контейнеров
          log "⏳ Ожидание запуска Docker контейнеров (45 сек)..."
          sleep 45
          
          # Проверка статуса контейнеров
          log "🔍 Проверка статуса Docker контейнеров..."
          
          # Проверка backend контейнера
          if docker ps | grep -q "estimate-backend-dev.*Up"; then
            success "✅ Backend контейнер запущен"
          else
            error "❌ Backend контейнер не запущен"
            docker logs estimate-backend-dev --tail 20 || true
          fi
          
          # Проверка frontend контейнера  
          if docker ps | grep -q "estimate-frontend-dev.*Up"; then
            success "✅ Frontend контейнер запущен"
          else
            error "❌ Frontend контейнер не запущен"
            docker logs estimate-frontend-dev --tail 20 || true
          fi
          
          # Проверка health check backend
          log "🏥 Проверка health check backend..."
          for i in {1..10}; do
            if docker exec estimate-backend-dev curl -f -s http://localhost:8000/api/v1/health/ > /dev/null 2>&1; then
              success "✅ Backend health check прошел"
              break
            fi
            if [ $i -eq 10 ]; then
              error "❌ Backend health check не прошел после 10 попыток"
              docker logs estimate-backend-dev --tail 30 || true
            fi
            sleep 3
          done
          
          # Проверка через внешние порты
          if curl -f -s http://localhost:${{ env.DEV_BACKEND_PORT }}/api/v1/health/ > /dev/null; then
            success "✅ Backend API отвечает на localhost:${{ env.DEV_BACKEND_PORT }}"
          else
            error "❌ Backend API не отвечает на localhost:${{ env.DEV_BACKEND_PORT }}"
          fi
          
          # Проверка через домен
          if curl -f -s https://dev.app.iqbs.pro/api/v1/health/ > /dev/null; then
            success "✅ Dev API доступен через https://dev.app.iqbs.pro"
          else
            error "❌ Dev API недоступен через домен"
          fi
          
          # Финальная проверка контейнеров
          log "🔍 Статус контейнеров:"
          docker ps | grep -E "(estimate-backend-dev|estimate-frontend-dev)" || echo "Контейнеры не найдены"
          
          log "📊 Использование ресурсов:"
          docker stats --no-stream estimate-backend-dev estimate-frontend-dev || true
          
          success "🎉 Быстрый деплой dev окружения завершен успешно!"
          echo ""
          echo "📍 Dev окружение доступно:"
          echo "   🌐 Web: https://dev.app.iqbs.pro" 
          echo "   🔌 API: https://dev.app.iqbs.pro/api/v1/"
          echo "   📊 Backend: http://localhost:${{ env.DEV_BACKEND_PORT }}"
        ENDSSH

    - name: 🎯 Финальная проверка деплоя
      run: |
        echo "🔍 Проверка доступности dev окружения..."
        
        # Проверка API health endpoint
        if curl -f -s https://dev.app.iqbs.pro/api/v1/health/ | grep -q "healthy"; then
          echo "✅ Dev API работает корректно"
        else
          echo "❌ Dev API недоступен"
          exit 1
        fi
        
        echo "🎉 Деплой dev окружения завершен успешно!"
        echo ""
        echo "📍 Деплой информация:"
        echo "   🌐 URL: https://dev.app.iqbs.pro"
        echo "   🔗 API: https://dev.app.iqbs.pro/api/v1/"
        echo "   📅 Время: $(date '+%Y-%m-%d %H:%M:%S UTC')"
        echo "   🚀 Коммит: ${{ github.sha }}"
        echo "   👤 Автор: ${{ github.actor }}"

    - name: 📊 Отчет об изменениях
      if: always()
      run: |
        echo "📋 Отчет о деплое:"
        echo "Измененные файлы:"
        echo "${{ steps.changes.outputs.changed_files }}"
        echo ""
        echo "Типы изменений:"
        echo "  Backend: ${{ steps.changes.outputs.backend_changed }}"
        echo "  Frontend: ${{ steps.changes.outputs.frontend_changed }}"
        echo "  Migrations: ${{ steps.changes.outputs.migrations_changed }}"